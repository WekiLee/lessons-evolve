# 变更日志

本项目遵循语义化版本思路记录重要变更。正式发布前的条目先归入“未发布”。

## 未发布

- 移除包安装阶段的自动写入行为，改为通过 CLI 显式安装。
- 增加 `package-lock.json`，发布流程使用 `npm ci --ignore-scripts`。
- 修正 Gemini 未配置环境变量时的显式安装错误处理。
- 补充多 Agent 安装说明、参数校验与开源治理文件。
