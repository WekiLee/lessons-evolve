# 安全政策

## 支持范围

当前仓库以最新主线版本为主要维护对象。正式发布版本后，安全修复优先覆盖最新稳定版本。

## 报告安全问题

请不要在公开讨论中粘贴密钥、令牌、个人身份信息或内部链接。公开仓库启用后，优先通过项目维护者指定的私密渠道报告安全问题；在私密渠道未配置前，可先创建不含敏感细节的 Issue，说明需要安全沟通。

## 处理原则

- 先确认影响范围，再公开细节。
- 修复方案默认维持最低必要安全基线，不引入超出问题范围的额外机制。
- 涉及安装脚本、文件写入路径和用户配置目录的变更，需要额外验证不会意外覆盖用户内容。
